# 任务计划：设置窗口重构与多语言支持

## 目标
- 将与润色相关的高级设置合并到“润色”页面。
- 删除或隐藏不必要的界面参数，保留清晰的 API 配置入口。
- API 润色配置尽量只需要 endpoint、key、模型；供应商类型自动识别，模型列表可自动获取。
- 完成代码验证、测试和文档同步。

## 阶段
1. [complete] 盘点当前实现和未完成改动
2. [complete] 修复/收口设置窗口和 LLM 客户端逻辑
3. [complete] 更新配置示例和 README
4. [complete] 运行编译与测试验证
5. [complete] 清理临时产物并给出结果
6. [complete] 将设置窗口改为单页滚动布局，底部保存/取消固定显示
7. [complete] 重新梳理设置/历史/状态的信息架构
8. [complete] 改为无额外弹窗的纵向主菜单 + 横向 tabs 布局
9. [complete] 设置瘦身：删除旧设置入口和死配置，隐藏工程参数
10. [complete] 发布收口：版本升到 0.0.7，更新发布日志并推送 GitHub
11. [complete] 增加多语言 UI、顶栏主题切换和软件说明弹窗
12. [complete] 修复英文按钮截断，压缩设置页重复说明
13. [complete] 将常见动作按钮图标化，并补充 tooltip
14. [complete] 修复设置窗口 DPI 启动路径，并优化可见渲染过程
15. [complete] 将主题切换改为原地换肤，并修复弹窗左上角闪现
16. [complete] 将润色 API 类型改为可选下拉框，并新增 OpenAI Responses 支持
17. [complete] 调整润色 API 操作顺序，并支持 endpoint 后台解析 base_url
18. [complete] 设计并执行润色/翻译模型评测：覆盖润色、润色+翻译、润色+翻译+原文三类场景，对比 gpt-5.4、gpt-5.4-mini、gpt-5.5、gpt-4o、gpt-4o-mini，并沉淀新版默认提示词
19. [complete] 重新设计录音交互：避开 Alt+Z 冲突，统一快捷键、托盘、预览浮窗和可拖动悬浮按钮的状态反馈
20. [complete] 发布收口：版本升到 0.0.8，更新 README、发布日志和忽略规则，完成测试后推送 GitHub
21. [complete] 增加私有网关兼容：支持可信 IP/自签名 provider 直连，并把 Host 头作为可选项
22. [complete] 参考 Handy 打磨悬浮录音按钮：接入真实音量电平，改为透明胶囊、音量条和处理动效
23. [complete] 整体收口悬浮图标视觉状态：共享渲染、42px 体系、离屏视觉 QA，并将旧文字预览浮窗改为默认关闭
24. [complete] 将旧文字预览浮窗重做为结果预览胶囊：同主悬浮按钮视觉、Windows per-pixel alpha、锚定在胶囊下方并默认开启
25. [complete] 修复悬浮 UI 卡住风险：热键业务回调改为顺序后台队列执行，预览胶囊静态态停止持续重绘
26. [complete] 二次修复润色完成后主悬浮胶囊卡在录音态：撤回主应用异步热键队列，增加悬浮状态序号和处理完成后的 idle 兜底
27. [complete] 三次修复主胶囊卡录音态：状态同步改为主悬浮按钮优先，托盘失败不影响悬浮 idle
28. [complete] 四次修复主胶囊卡录音态：原生浮窗状态入队后主动 PostMessage 唤醒，并在 tick 中按主线程最新状态自校准
29. [complete] 五次修复主胶囊卡处理中态和拖不动：最终文本出现即收回 idle，拖动期间不再被 mouse leave 清除 pressed
30. [complete] 结构化修复拖动位置结束后回弹：将保存位置定义为空闲小圆 resting position，展开/收回统一右边缘锚定，并在 idle reset/rebuild 前反算保存当前位置
31. [complete] 悬浮胶囊适配主界面深浅配色：抽出共享主题 palette，悬浮按钮和结果预览从主界面语义色派生，并支持设置窗口主题按钮实时同步/取消回滚
32. [complete] 微调浅色胶囊可见性：浅色浮窗底色从纯白改为主界面浅灰层，hover 使用更深浅灰层以增强边界
33. [complete] 微调浅色未展开小圆：空闲 42px 小圆使用同一浅灰层级和不透明底，避免默认态偏白
34. [complete] 结构化修复 Tk/Tcl 原生崩溃风险：更新弹窗改走 Win32 MessageBox，所有 Tk root/mainloop 入口进入进程级守卫，日志窗口关闭即释放 Tk
35. [complete] 发布收口：版本升到 0.0.9，匿名化私有 provider 记录，更新 README 和发布日志，完成测试后推送 GitHub
36. [complete] 仓库过时内容清理：删除旧 `scripts/post_build.py` 增量发布脚本，更新 Claude/UI/style/release/build/README 说明，统一当前默认模型和 Python 版本。
37. [complete] 拆分设置和运行数据：设置窗口只保留历史保存策略，历史浏览/复制/清空和会话状态迁移到独立“运行数据”窗口，托盘入口改为打开运行数据。
38. [complete] 修复多显示器显示与拖拽体验：设置/运行数据窗口按鼠标所在显示器缩放并居中，悬浮按钮可拖到完整虚拟桌面，悬浮按钮与结果预览胶囊按所在显示器 UI scale 渲染。

## 决策
- UI 保持简单，但润色 API 类型需要保留为可选下拉框：自动识别、OpenAI Chat Completions、OpenAI Responses、Anthropic Messages。
- 内部仍保留 Azure OpenAI、OpenAI-compatible、Anthropic 的兼容层。
- 保存后的润色 profile 收敛为通用字段：provider、endpoint、api_key、model。
- 设置窗口采用左侧锚点导航 + 右侧单页滚动；保存/取消按钮固定在底部。
- 设置窗口只放可配置项；运行数据类内容从设置页移出。
- 历史浏览、复制、清空和轻量会话状态由独立“运行数据”窗口承接，并继续通过进程级 Tk root 守卫串行运行。
- 托盘“运行数据”入口打开独立运行数据窗口，不再复用设置窗口或定位到设置页历史 tab。
- 设置瘦身完成：主设置页收敛为转写模型、润色连接、快捷键、历史；旧 Web 配置向导、唤醒词实验模块和未生效配置项已移除。
- 多语言支持先采用轻量 source-text 字典，默认中文，英文作为第一种额外界面语言；后续可继续扩展语言包。
- 主题切换和 Info 入口移到设置窗口右上角；主题/语言作为 `ui` 配置持久化。
- 英文 UI 的按钮宽度按文本自动放大；设置页描述只保留标题、状态和必要操作提示。
- 图标化优先用于刷新、复制、下载、删除、展开、主题和 Info；保存、取消、验证等关键动作保留文字并加图标。
- 调试/试用设置窗口改走 `run.py --open-settings`，避免绕过 Windows DPI awareness。
- Tkinter 暂时继续保留，但通过隐藏重建、图标预热和历史页懒加载降低可见渲染成本；中长期若继续追求原生质感和性能，优先考虑 PySide6/Qt。
- 颜色主题切换只做原地换肤，不再销毁/重建整个设置窗口；语言切换仍可重建，因为文本、布局和控件宽度都会变化。
- 润色 API 类型现在由用户可选：自动识别、OpenAI Chat Completions、OpenAI Responses、Anthropic Messages。显式选择后验证/保存不再被自动识别改成其他协议。
- 润色 API 操作顺序调整为先获取模型、再验证；用户可在 endpoint 中省略 `/v1`，验证/获取模型会将解析出的实际 base URL 保存到 `base_url`，但设置窗口继续显示用户输入的 endpoint。
- 录音入口应从“只有全局快捷键”升级为“快捷键 + 屏幕悬浮按钮”双入口；两者必须共用同一套开始、停止、取消和状态更新逻辑。
- 默认不再推荐 `Alt+Z`；Windows 上优先推荐 `Ctrl+Shift+Space` 或 `Ctrl+Alt+Space`，并在设置里对常见冲突组合给出明确警告。
- v0.0.8 以“悬浮录音按钮 + 多语言/配色 UI + 润色 API/Prompt 升级”为发布主题。
- 私有网关兼容采用 profile 级 `allow_insecure_tls`，不做全局网络开关；Host 头为可选字段，只有 IP 直连但后端需要原域名路由时填写。
- 不引入单独 CA bundle / 证书链配置；用户当前需求是“让可信自有网关可用”，不是证书管理。
- 悬浮按钮的“灵动”方向以连续状态反馈、低侵入和原生渲染质感为主：Windows 上用 layered window/per-pixel alpha 保证圆角边缘，空闲时是小浮点，hover/状态消息时展开，录音时用真实 RMS 音量条和计时，处理中用轻量旋转/呼吸反馈；录音状态机仍只放在 `AIInputApp`，浮窗不复制业务逻辑。
- 结果预览不再使用旧文字气泡：保留 `PreviewOverlay` 能力，但视觉上改为和主悬浮按钮一致的结果预览胶囊；主胶囊负责入口/录音控制，预览胶囊负责转写文本、最终结果和降级提示。
- 热键监听层只负责识别组合键和维护按下/松开状态；业务回调必须在锁外执行，但主应用不使用完全异步的热键业务队列，避免 release 后的滞后按键事件触发二次录音。
- 主悬浮胶囊是最高优先级状态反馈，托盘只是辅助反馈；状态同步时先推主悬浮按钮，再更新托盘。
- 原生悬浮窗跨线程状态同步采用“队列 + PostMessage 唤醒 + tick 自校准”，避免旧 recording 帧因消息延迟或队列处理异常长期停留。
- 最终结果由结果预览胶囊承担；主悬浮胶囊在最终文本出现时立即回 idle，不等待后台收尾动作。
- 悬浮按钮位置只保存空闲小圆左上角；任何展开状态都从该 resting position 右边缘向左展开，拖动释放和原生 idle rebuild 都必须先反算回 resting position 再持久化。
- 主题色以 `src/ui_theme.py` 的主界面语义 palette 为源头；悬浮按钮/结果预览只做透明浮窗需要的派生色，不能再各自维护脱节的深浅色常量。
- 设置窗口主题按钮属于即时预览操作：常驻浮窗同步切换，但保存前不写入配置；若用户取消设置窗口，运行时浮窗要回滚到打开设置前的主题。
- 浅色浮窗使用主界面 `surface2` 而非纯白 `surface`，避免常驻胶囊在浅色桌面或浅色应用上轮廓过弱。
- 浅色未展开小圆也使用浮窗浅灰派生色，而不是退回纯白或半透明白底。
- Tk/Tcl 在 Windows 上只能作为受控的 fallback/设置 UI 使用：所有独立 `tk.Tk()` root 必须通过进程级守卫创建，禁止在后台流程里随手创建临时 Tk root。
- 更新/错误确认这类简单弹窗在 Windows 上走原生 MessageBox，不占用 Tk root；非 Windows fallback 才使用 Tk 守卫。
- 日志窗口不再保持隐藏 Tk root 常驻；关闭日志窗即销毁，下次从托盘打开时再创建，避免阻塞设置窗口和其他 Tk fallback。
- 发布前必须把项目记录、评测样例和测试里的真实私有 provider 域名/IP 匿名化，只保留 `llm.example.com`、`203.0.113.10` 等文档占位值；`config.yaml` 和本机 endpoint/key 绝不提交。

## 下一阶段建议
- 观察 v0.0.9 在真实常驻使用中的稳定性，重点看 Tk root 守卫、悬浮胶囊状态回落、拖动位置持久化和结果预览显示节奏。
- 继续打磨运行数据窗口：可逐步加入更有用的诊断信息，例如最近失败原因、当前 provider/模型、最近一次 fallback 状态和日志入口。
- 更新体验可优先打磨应用内更新：当前 release 以安装包为主，`updater.py` 只保留旧增量包兼容路径；后续可决定是否让便携 zip 也成为一等更新目标。
- 润色/翻译评测工具链已建立，后续模型或 prompt 调整应继续用 `scripts/eval_polish.py` 做对照，而不是直接盲替换默认 prompt。
