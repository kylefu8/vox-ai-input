# 发现记录

## 当前上下文
- 用户允许破坏性重构，不需要兼容已有用户。
- 在线 STT 已移除，本地转录是唯一方向。
- 润色能力需要支持多 endpoint，但 UI 要降低复杂度。

## 已确认
- 设置窗口里“高级”页入口已移除，润色提示词/语言设置已经并入“润色”页。
- LLM 自动识别已有单元测试；模型列表获取新增合并去重测试。
- 单页滚动后，“概览”和完整历史记录放在设置页面里显得不自然。
- 历史记录需要独立浏览/复制入口，设置里只需要保留启用开关和保留条数。
- 旧 `--setup` Web 配置向导已经落后于新 LLM profile 结构，继续保留会产生错误配置。
- `output.paste_method` 和 `wakeword` 是配置模板里的外露能力，但当前主流程没有真正读取或暴露对应运行入口，已从默认配置中移除；未引用的 `wakeword.py` 也已删除。
- 推理线程数、流式开关、识别语言和润色 prompt 是高级/工程参数；主设置页应该默认隐藏或自动派生。
- Handy 的多语言思路适合借鉴为“界面语言是普通偏好项”，但本项目当前更适合轻量字典而不是引入完整翻译平台。
- 主题切换若通过销毁重建 Tk 内容实现，必须先同步未保存表单状态，否则用户改完 API key 再切主题会丢输入；本轮已加重建前快照。
- Tk Button 的 `width` 是字符位，原先为中文短词设置的 `w=4` 会截断英文 `Download`；按钮宽度应按实际文本自动扩展。
- 设置页里页面副标题、分区副标题、卡片提示同时存在时会显得啰嗦；本轮保留标题/状态/操作，删除重复说明。
- 图标按钮适合放在重复列表和工具栏动作中；保存/取消/验证这类改变全局状态的按钮保留文字更稳妥。
- PowerShell 默认 GBK 控制台不能稳定打印某些 Unicode 图标；烟测输出应避免直接打印图标字符。
- Emoji 图标需要忽略 U+FE0E/U+FE0F 等变体选择符参与宽度估算，否则 Tk 按钮会被无意义撑宽。
- Windows/Tk 的原生 emoji 文本渲染不稳定，可能显示成黑白轮廓；需要使用 Pillow 生成彩色位图图标才能保证用户看到的效果一致。
- Tkinter 多线程窗口中创建 `ImageTk.PhotoImage` 必须显式传入 `master=root`，否则会出现 `image "pyimage..." doesn't exist`。
- `pystray` 需要在创建 `pystray.Icon` 的作用域内导入；只在 helper 函数里局部导入不够。
- Windows 上如果绕过 `run.py` 直接 `python -c` 创建应用，会跳过 `_configure_windows_dpi()`，高缩放屏下 Tk 窗口容易被系统位图缩放，看起来发虚。
- Tkinter 的主题/语言切换若在可见窗口中销毁重建大量控件，会明显看到逐项渲染；重建期间先 `withdraw()`，完成后再 `deiconify()` 可明显降低肉眼闪烁。
- 历史记录列表是设置窗口里最容易产生大量控件的区域，应按需加载，不应在默认打开转写页时同步构建。
- 如果后续要彻底解决 UI 质感、渲染效率和复杂设置页组织问题，PySide6/Qt 是比继续深挖 Tkinter 更稳的长期方向；CustomTkinter 迁移小但性能上仍受 Tk 约束。
- 主题切换如果只改变配色，不应该销毁重建窗口；对当前 Tk 架构来说，可通过递归映射旧主题色到新主题色完成原地换肤。
- Tk 的新 `Toplevel` 会先出现在系统默认位置，通常是屏幕左上角；弹窗应先 `withdraw()`，构建并计算 geometry 后再 `deiconify()`，否则会出现从左上角“飞过来”的观感。
- 深色主题不宜接近纯黑；在设置密度较高的工具界面里，柔和炭灰背景比黑底更耐看，也能让卡片/输入框层级更自然。
- OpenAI-compatible 代理的根路径可能不是 API base，例如 `https://llm.haph.fun/` 根路径返回状态页，而真正的 chat/models API 在 `/v1`；自动探测应优先尝试 `/v1`。
- 模型列表包含某个模型不代表当前账号/分组一定能调用该模型；`llm.haph.fun` 对 `claude-sonnet-4.6` 返回“分组 svip 下模型无可用渠道（distributor）”，但同一 endpoint/key 调用 `gpt-4o-mini` 成功。
- 同一个 endpoint/provider 可能同时暴露 Chat Completions 和 Responses API，但不同模型只支持其中一种；API 类型不能只靠自动识别一次后固定，应允许用户手动选择。
- `llm.haph.fun` 明确返回过 “Model gpt-4o-mini cannot be reached via /responses. Supported backend(s): /chat/completions.”，说明代理不会在 `/responses` 和 `/chat/completions` 之间自动转换。
- 设置页里“获取模型”应位于“验证”左侧：用户通常先通过 endpoint/key 获取模型列表，再选择模型并验证实际调用。
- endpoint 显示值和运行时 base URL 应分离：用户输入根地址时，UI 保留原文；后台解析出的 `/v1` 应保存为 `base_url` 并由运行时优先使用。
- 当前润色 prompt 适合“最小化后处理”的早期目标，但评测应重点观察三类新风险：翻译模式下是否过度润色、双输出是否稳定保持两段格式、中英混杂时是否误翻专有名词/代码/API 术语。
- 这次模型对比应同时记录“质量”和“工程可用性”：输出质量、格式合规、延迟、失败率、是否支持所选 API 类型，以及同一 endpoint 下模型实际可调用情况。
- `llm.haph.fun` 在当前网络下不是服务不可用，而是系统 DNS/SNI 路径异常：系统 DNS 返回 `6.6.6.33`，HTTP 会出现上海蓝云阻断页；DoH 返回 `40.73.121.73`。直接用 IP + `Host: llm.haph.fun` 可以访问服务，但需要跳过 TLS 主机名校验。
- `llm.haph.fun` 上 `gpt-5.4`、`gpt-5.4-mini`、`gpt-5.5` 在评测中自动识别为 OpenAI Responses；`gpt-4o`、`gpt-4o-mini` 自动识别为 OpenAI Chat Completions。
- Responses 类模型做连通性验证时需要比 16 更高的 `max_output_tokens`，因为可能先消耗 reasoning tokens；验证上限已调整为 64。
- 代表性矩阵中，润色、英译、双语输出格式都稳定；英译中单输出场景的自动问题集中在技术词保护，尤其是 `endpoint` / `Responses API` / `chat completions` 是否应保留原文还是翻译为“端点/响应 API/聊天补全”。
- 第二轮长文本后，补强术语保留 prompt 有效：英译中长文本里 `endpoint`、`base_url`、`Responses API`、`Chat Completions`、`API key`、模型名基本能保留。
- 长文本暴露的新风险：当输入本身包含“帮我整理成会议纪要”这类显式改写指令，同时场景要求翻译为英语时，`gpt-4o` 和 `gpt-4o-mini` 更容易优先执行原文中的中文整理请求，而忽略外层翻译目标；`gpt-5.4`、`gpt-5.4-mini`、`gpt-5.5` 在该场景更可靠。
- `gpt-5.4` 在明确会议纪要场景中倾向输出过多空行；质量不差，但会降低粘贴后的紧凑性。
- `gpt-4o` 在会议纪要润色中会使用 `**加粗**` 等 Markdown 风格，和“只输出纯文本”的产品目标不完全一致；后续自动检查应增加 Markdown 标记检测。
- 长文本综合看，`gpt-5.4-mini` 仍是默认模型的稳妥选择；`gpt-5.5` 质量最稳但延迟明显更高，适合作为高质量模式；`gpt-4o-mini` 适合速度优先但不适合作为复杂翻译默认。
- 现有录音交互是典型“长按说话”：热键 press 开始录音，release 停止并处理，Esc 取消；这适合快速输入，但一旦热键冲突，用户缺少替代入口。
- `Alt+Z` 在 Windows 桌面环境中容易和显卡覆盖层、游戏/录屏工具或应用内部快捷键冲突；设置页应把它加入高风险提示，不作为推荐组合。
- 悬浮控制不应独立复制录音逻辑，应调用和热键相同的开始/停止/取消方法，并接受统一状态广播；否则会出现托盘显示空闲但浮动按钮还在录音等不一致。
- 现有 `PreviewOverlay`、`CountdownOverlay` 已证明“后台线程 + queue + topmost/no-activate 浮窗”在项目里可行；新的悬浮按钮可以沿用这个线程模型，但需要额外支持拖动、位置持久化和点击命令。
- 悬浮按钮实现时要保持“薄控制器”定位：它只负责 UI、拖动和回调，不能自己判断转写/润色流程；否则会和热键长按、自动停止、Esc 取消产生重复状态机。
- 在 Windows 上拖动悬浮按钮时，位置持久化可以直接写入当前配置；但如果用户的润色配置处于未完成状态，`save_config` 可能因为 LLM profile 校验失败，因此位置保存要捕获异常并降级为仅本次会话生效。
- 可见窗口验证上，overrideredirect 的 Tk 悬浮窗仍可通过 Win32 标题枚举确认存在；本轮为悬浮窗设置了 `Vox Floating Control` 标题，方便后续烟测。
- 用户真实例句说明“高保真/保守润色”不适合默认体验：当本地 STT 已经能识别大意但有大量语音残留时，默认 prompt 应该更像“标准清理”，主动去重复、去无意义连接词、修正明显中英混合误识别。
- 对专名和内部缩写的边界应保持：`晶天`、`BB`、`OW` 这类无法确定的词不应猜测替换，但 `ton尼`、`review了`、`hand一下` 这种高度明确的中英混杂可以规范为 `Tony`、`review 了`、`handle 一下`。
- “润色无效果”有两种完全不同原因：一种是 prompt 太保守，一种是 API 失败后返回原文。后者必须在 UI/历史中显性标记，否则用户无法判断是模型质量问题还是连接问题。
