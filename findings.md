# 发现记录

## 当前上下文
- 用户允许破坏性重构，不需要兼容已有用户。
- 在线 STT 已移除，本地转录是唯一方向。
- 润色能力需要支持多 endpoint，但 UI 要降低复杂度。

## 已确认
- 设置窗口里“高级”页入口已移除，润色提示词/语言设置已经并入“润色”页。
- LLM 自动识别已有单元测试；模型列表获取新增合并去重测试。
- 单页滚动后，“概览”和完整历史记录放在设置页面里显得不自然。
- 历史记录需要独立浏览/复制入口，设置里只需要保留启用开关和保留条数。
- 旧 `--setup` Web 配置向导已经落后于新 LLM profile 结构，继续保留会产生错误配置。
- `output.paste_method` 和 `wakeword` 是配置模板里的外露能力，但当前主流程没有真正读取或暴露对应运行入口，已从默认配置中移除；未引用的 `wakeword.py` 也已删除。
- 推理线程数、流式开关、识别语言和润色 prompt 是高级/工程参数；主设置页应该默认隐藏或自动派生。
- Handy 的多语言思路适合借鉴为“界面语言是普通偏好项”，但本项目当前更适合轻量字典而不是引入完整翻译平台。
- 主题切换若通过销毁重建 Tk 内容实现，必须先同步未保存表单状态，否则用户改完 API key 再切主题会丢输入；本轮已加重建前快照。
- Tk Button 的 `width` 是字符位，原先为中文短词设置的 `w=4` 会截断英文 `Download`；按钮宽度应按实际文本自动扩展。
- 设置页里页面副标题、分区副标题、卡片提示同时存在时会显得啰嗦；本轮保留标题/状态/操作，删除重复说明。
- 图标按钮适合放在重复列表和工具栏动作中；保存/取消/验证这类改变全局状态的按钮保留文字更稳妥。
- PowerShell 默认 GBK 控制台不能稳定打印某些 Unicode 图标；烟测输出应避免直接打印图标字符。
- Emoji 图标需要忽略 U+FE0E/U+FE0F 等变体选择符参与宽度估算，否则 Tk 按钮会被无意义撑宽。
- Windows/Tk 的原生 emoji 文本渲染不稳定，可能显示成黑白轮廓；需要使用 Pillow 生成彩色位图图标才能保证用户看到的效果一致。
- Tkinter 多线程窗口中创建 `ImageTk.PhotoImage` 必须显式传入 `master=root`，否则会出现 `image "pyimage..." doesn't exist`。
- `pystray` 需要在创建 `pystray.Icon` 的作用域内导入；只在 helper 函数里局部导入不够。
- Windows 上如果绕过 `run.py` 直接 `python -c` 创建应用，会跳过 `_configure_windows_dpi()`，高缩放屏下 Tk 窗口容易被系统位图缩放，看起来发虚。
- Tkinter 的主题/语言切换若在可见窗口中销毁重建大量控件，会明显看到逐项渲染；重建期间先 `withdraw()`，完成后再 `deiconify()` 可明显降低肉眼闪烁。
- 历史记录列表是设置窗口里最容易产生大量控件的区域，应按需加载，不应在默认打开转写页时同步构建。
- 如果后续要彻底解决 UI 质感、渲染效率和复杂设置页组织问题，PySide6/Qt 是比继续深挖 Tkinter 更稳的长期方向；CustomTkinter 迁移小但性能上仍受 Tk 约束。
- 主题切换如果只改变配色，不应该销毁重建窗口；对当前 Tk 架构来说，可通过递归映射旧主题色到新主题色完成原地换肤。
- Tk 的新 `Toplevel` 会先出现在系统默认位置，通常是屏幕左上角；弹窗应先 `withdraw()`，构建并计算 geometry 后再 `deiconify()`，否则会出现从左上角“飞过来”的观感。
- 深色主题不宜接近纯黑；在设置密度较高的工具界面里，柔和炭灰背景比黑底更耐看，也能让卡片/输入框层级更自然。
- OpenAI-compatible 代理的根路径可能不是 API base，例如 `https://llm.example.com/` 根路径返回状态页，而真正的 chat/models API 在 `/v1`；自动探测应优先尝试 `/v1`。
- 模型列表包含某个模型不代表当前账号/分组一定能调用该模型；某些私有网关会对特定模型返回“分组下模型无可用渠道（distributor）”，但同一 endpoint/key 调用其他模型成功。
- 同一个 endpoint/provider 可能同时暴露 Chat Completions 和 Responses API，但不同模型只支持其中一种；API 类型不能只靠自动识别一次后固定，应允许用户手动选择。
- 私有网关明确返回过 “Model gpt-4o-mini cannot be reached via /responses. Supported backend(s): /chat/completions.”，说明代理不会在 `/responses` 和 `/chat/completions` 之间自动转换。
- 设置页里“获取模型”应位于“验证”左侧：用户通常先通过 endpoint/key 获取模型列表，再选择模型并验证实际调用。
- endpoint 显示值和运行时 base URL 应分离：用户输入根地址时，UI 保留原文；后台解析出的 `/v1` 应保存为 `base_url` 并由运行时优先使用。
- 当前润色 prompt 适合“最小化后处理”的早期目标，但评测应重点观察三类新风险：翻译模式下是否过度润色、双输出是否稳定保持两段格式、中英混杂时是否误翻专有名词/代码/API 术语。
- 这次模型对比应同时记录“质量”和“工程可用性”：输出质量、格式合规、延迟、失败率、是否支持所选 API 类型，以及同一 endpoint 下模型实际可调用情况。
- 私有网关在异常网络下可能不是服务不可用，而是系统 DNS/SNI 路径异常：系统 DNS 返回错误地址，HTTP 被拦截；另一路解析返回真实地址。直接用文档示例 IP `203.0.113.10` + `Host: llm.example.com` 可以表达这种场景，但需要跳过 TLS 主机名校验。
- 私有网关上某些新模型在评测中自动识别为 OpenAI Responses；`gpt-4o`、`gpt-4o-mini` 自动识别为 OpenAI Chat Completions。
- Responses 类模型做连通性验证时需要比 16 更高的 `max_output_tokens`，因为可能先消耗 reasoning tokens；验证上限已调整为 64。
- 代表性矩阵中，润色、英译、双语输出格式都稳定；英译中单输出场景的自动问题集中在技术词保护，尤其是 `endpoint` / `Responses API` / `chat completions` 是否应保留原文还是翻译为“端点/响应 API/聊天补全”。
- 第二轮长文本后，补强术语保留 prompt 有效：英译中长文本里 `endpoint`、`base_url`、`Responses API`、`Chat Completions`、`API key`、模型名基本能保留。
- 长文本暴露的新风险：当输入本身包含“帮我整理成会议纪要”这类显式改写指令，同时场景要求翻译为英语时，`gpt-4o` 和 `gpt-4o-mini` 更容易优先执行原文中的中文整理请求，而忽略外层翻译目标；`gpt-5.4`、`gpt-5.4-mini`、`gpt-5.5` 在该场景更可靠。
- `gpt-5.4` 在明确会议纪要场景中倾向输出过多空行；质量不差，但会降低粘贴后的紧凑性。
- `gpt-4o` 在会议纪要润色中会使用 `**加粗**` 等 Markdown 风格，和“只输出纯文本”的产品目标不完全一致；后续自动检查应增加 Markdown 标记检测。
- 长文本综合看，`gpt-5.4-mini` 仍是默认模型的稳妥选择；`gpt-5.5` 质量最稳但延迟明显更高，适合作为高质量模式；`gpt-4o-mini` 适合速度优先但不适合作为复杂翻译默认。
- 现有录音交互是典型“长按说话”：热键 press 开始录音，release 停止并处理，Esc 取消；这适合快速输入，但一旦热键冲突，用户缺少替代入口。
- `Alt+Z` 在 Windows 桌面环境中容易和显卡覆盖层、游戏/录屏工具或应用内部快捷键冲突；设置页应把它加入高风险提示，不作为推荐组合。
- 悬浮控制不应独立复制录音逻辑，应调用和热键相同的开始/停止/取消方法，并接受统一状态广播；否则会出现托盘显示空闲但浮动按钮还在录音等不一致。
- 现有 `PreviewOverlay`、`CountdownOverlay` 已证明“后台线程 + queue + topmost/no-activate 浮窗”在项目里可行；新的悬浮按钮可以沿用这个线程模型，但需要额外支持拖动、位置持久化和点击命令。
- 悬浮按钮实现时要保持“薄控制器”定位：它只负责 UI、拖动和回调，不能自己判断转写/润色流程；否则会和热键长按、自动停止、Esc 取消产生重复状态机。
- 在 Windows 上拖动悬浮按钮时，位置持久化可以直接写入当前配置；但如果用户的润色配置处于未完成状态，`save_config` 可能因为 LLM profile 校验失败，因此位置保存要捕获异常并降级为仅本次会话生效。
- 可见窗口验证上，overrideredirect 的 Tk 悬浮窗仍可通过 Win32 标题枚举确认存在；本轮为悬浮窗设置了 `Vox Floating Control` 标题，方便后续烟测。
- 用户真实例句说明“高保真/保守润色”不适合默认体验：当本地 STT 已经能识别大意但有大量语音残留时，默认 prompt 应该更像“标准清理”，主动去重复、去无意义连接词、修正明显中英混合误识别。
- 对专名和内部缩写的边界应保持：`晶天`、`BB`、`OW` 这类无法确定的词不应猜测替换，但 `ton尼`、`review了`、`hand一下` 这种高度明确的中英混杂可以规范为 `Tony`、`review 了`、`handle 一下`。
- “润色无效果”有两种完全不同原因：一种是 prompt 太保守，一种是 API 失败后返回原文。后者必须在 UI/历史中显性标记，否则用户无法判断是模型质量问题还是连接问题。
- 主应用也需要支持“域名解析异常但 IP 可达”的私有网关场景；不能只把 IP/Host/TLS workaround 留在评测脚本里，否则设置页获取模型和真实润色会继续失败。
- 对用户当前场景来说，Host 头不是必填项：如果后端按 IP 访问也能路由，`host_header` 留空即可；只有后端依赖原域名路由/SNI 替代逻辑时才需要填写。
- 不应在设置页引入单独 CA bundle / 自签名证书链管理；当前产品目标是轻量 Windows 工具，用户明确不希望单独维护证书验证配置。对可信私有网关保留一个场景化的“IP/自签名兼容”开关更合适。
- `allow_insecure_tls` 必须是 profile 级别，而不是全局网络开关；它只影响当前润色 provider 的模型列表、验证和运行时请求，避免降低 GitHub 更新检查或其他网络请求的安全性。
- Handy 的浮窗“灵动感”主要来自连续反馈，而不是更复杂的功能：透明胶囊形态、录音音量条、状态淡入/呼吸和取消按钮按压反馈，比单纯扩大按钮或堆说明文字更适合当前 Windows 小工具。
- 对 Vox 这种常驻入口，空闲态不应长期占用一整条胶囊；更合适的是空闲小浮点，hover、提示消息、录音和处理时再展开成胶囊，降低屏幕侵入感。
- Tk 的 `-transparentcolor` 是二值透明，圆角抗锯齿边缘会天然粗糙；如果 Windows 浮窗要有高质感圆角，必须走 layered window / per-pixel alpha，不能继续靠 Tk 透明色修补。
- 原生 layered window 渲染需要 premultiplied BGRA；Pillow RGBA 直接塞进 DIB 会产生错误混合。当前实现用 numpy 做 alpha 预乘后调用 `UpdateLayeredWindow`。
- 手画麦克风图标在小尺寸下容易显得廉价；Windows 自带 `Segoe MDL2 Assets` 图标 glyph 在小浮窗里更稳。
- Win32 ctypes 代码必须显式声明 64 位句柄函数签名；否则 Python 默认按 32 位 int 传参，会导致原生窗口创建/绘制失败并静默回退到 Tk。
- 用低透明 RGBA 形状直接画在同一 Pillow 图层上，可能在小尺寸控件里把底色视觉上“挖薄”；浮窗内部底座应使用实心深色，再叠高对比 glyph。
- 默认收起态如果还保留“外壳 + 内圆 + 图标”，在 40px 级别会显得拥挤和廉价；单层圆形底 + 一个 glyph 的结构更干净。
- Vox 浮窗可以从录音回调中拿轻量 RMS 电平，但必须在主控制器节流后再进 Tk 队列；音频回调线程里只做 `chunk` 拷贝、RMS 计算和短回调，不能做 Tk 或阻塞操作。
- 悬浮按钮继续保持“薄控制器”：真实开始/停止/取消仍由 `AIInputApp` 统一状态机负责，音量条只是状态展示，不应反向驱动录音流程。
- 悬浮图标这种高频可见控件不适合让 Win32 和 Tk 两个分支各画一套 UI；共享渲染函数能保证原生路径、fallback 和离屏 QA 看到的是同一套像素。
- Windows layered window 在自动截屏工具里可能只抓到背景，不能把普通截图是否包含窗口当作唯一验证标准；更可靠的是结合窗口类名/尺寸枚举、离屏渲染图和真实交互肉眼检查。
- 旧 `PreviewOverlay` 不应和悬浮麦克风合并：麦克风浮窗负责入口、录音/处理状态和取消；文字预览负责展示转写原文、润色结果、失败降级和流式中间文本。若嫌干扰，应加独立开关或重做视觉，而不是直接删除。
- 新版悬浮胶囊完成录音/处理/完成状态表达后，旧文字预览浮窗默认出现会显得重复且打断视觉层级；更合适的产品策略是默认关闭，保留配置开关给流式转写调试或未来重做结果预览。
- 当结果预览被重做为同一视觉体系后，它可以重新默认开启：主悬浮胶囊负责录音入口和即时状态，结果预览胶囊负责文本内容、降级提示和流式中间结果；两者视觉一致但职责分开，减少重复感。
- 结果预览这种圆角浮窗也应和主悬浮按钮一样使用 Windows layered window / per-pixel alpha；Tk `-transparentcolor` 只能作为 fallback，否则在深色圆角边缘仍容易出现毛边。
- 热键监听线程不应在持锁状态里直接执行录音开始/停止回调；停止录音会碰音频流、提示音、预览/悬浮 UI 和后台处理线程，放在监听回调里容易造成释放事件或后续状态看起来卡住。更稳妥的是热键状态机只记录 press/release，并用顺序后台队列执行业务回调。
- 预览胶囊的结果/警告态是静态信息，不需要每帧重绘；只有处理中圆环需要动画。减少无意义重绘能降低和主悬浮按钮、Tk 设置窗口同时运行时的 UI 压力。
- 热键业务回调不应在持锁状态执行，但主录音入口也不适合完全异步化：release 后若立即把热键状态标为未激活，键盘重复/滞后事件可能排入二次 activate，让主悬浮按钮在润色完成后又回到 recording。当前更稳的边界是“锁外同步执行回调”，再用主控制器的最终 idle 兜底确保视觉状态落回空闲。
- 对常驻悬浮 UI，状态命令应带单调序号；即使后续有跨线程队列延迟，也不应让较旧的 recording/processing 状态覆盖较新的 idle。
- 主悬浮胶囊是用户最直接看到的状态源，状态同步时应优先更新它，再做托盘这种辅助反馈；pystray 图标/标题更新如果阻塞或异常，不应阻止主悬浮胶囊收到 idle。
- 原生悬浮窗口的跨线程状态更新不应只依赖 `WM_TIMER` 轮询队列；状态入队后应主动 `PostMessage` 唤醒窗口过程，并在 UI 线程 tick 中以主线程的最新状态作自校准，防止某次队列处理异常或消息延迟让 recording 旧帧永久停留。
- 主胶囊不应等整个后台收尾完成才离开 processing；最终文本 callback 出现时，结果预览已经承担最终反馈，主胶囊应立即收回 idle。否则日志、短暂停留、preview dismiss、临时文件清理任一步慢下来，用户都会看到“处理中”假卡住。
- 原生小浮窗拖动时不要在 `WM_MOUSELEAVE` 里清除 pressed；鼠标捕获期间离开窗口是正常拖动路径，提前清 pressed 会让 processing/recording 等小胶囊看起来拖不动。
- 悬浮按钮的位置必须保存为“空闲小圆”的 resting position，而不是当前渲染窗口的左上角；录音/处理中胶囊宽度不同，如果直接保存展开窗口 left，结束后收回 42px 小圆时就会看起来跳回或偏移。所有状态应通过“空闲右边缘锚定展开”和“展开矩形反算空闲坐标”互相转换。
- 常驻浮窗不应维护一套与设置窗口脱节的颜色常量；否则深浅色切换时主界面和胶囊会像两个产品。设置窗口、悬浮按钮和结果预览应共用语义主题色，浮窗只做适合透明 layered window 的派生。
- 设置窗口的主题切换是即时视觉操作，如果要让外部浮窗同步，也需要处理取消语义：主题按钮点击时先实时预览到常驻胶囊，保存后持久化；用户取消/关闭设置时应通知胶囊回到打开设置前的主题。
- 浅色主题下常驻浮窗不适合直接使用主设置页的纯白 `surface`；在桌面和浅色应用上边缘会变弱。浮窗/预览胶囊应使用浅灰 `surface2` 作为底，hover 再进入 `rail` 层级，仍然与主界面同源但更容易看清。
- 浅色空闲小圆比展开胶囊更容易显白，因为可见面积小且背景透明；需要单独使用 `idle_bg` 浅灰底，并保持不透明填充，才能让未展开状态和展开胶囊观感一致。
- Windows 上多个独立 `tk.Tk()` root 分散在不同线程时，失败可能表现为 `tcl86t.dll` 原生崩溃或应用 hung，而不是 Python traceback；不能只修某一个窗口实例，必须统一管理所有 Tk root 生命周期。
- 后台线程里为了弹一个消息框临时创建 `tk.Tk()` 是高风险模式，尤其会和设置窗口、日志窗口等长期 Tk mainloop 并存；Windows 上应优先使用原生 `MessageBoxW`，非 Windows fallback 再进入 Tk 守卫。
- 长期隐藏但不销毁的 Tk 窗口会让进程级 Tk 守卫变成新的阻塞点；日志类辅助窗口更适合关闭即销毁，下次打开再重建。
- 设置窗口在等待 Tk 守卫前就应标记 `_settings_open`，否则连续点击可能排出多个等待线程，等守卫释放后再依次创建多个设置窗口。
- `scripts/post_build.py` 已脱离当前发布链路：GitHub Actions 直接生成 `VoxAIInput-*-win64.zip`、安装包和 `update-manifest.json`，release 不再发布 `app-update.zip`。旧增量更新逻辑可以留在 `src/updater.py` 做历史兼容，但脚本和构建说明不应继续作为当前步骤出现。
- 历史 release note 中的旧模型名应保留为历史记录；当前可执行代码、示例和测试里的默认/推荐模型应统一到 `gpt-5.4-mini`，避免新建 profile 时继续带出旧默认。
- 项目记忆文件本身也会过时：`task_plan.md` 里已经完成的评测/悬浮控制“下一阶段计划”如果不改，会误导后续 session 以为这些仍待执行；完成后应替换为新的后续建议。
